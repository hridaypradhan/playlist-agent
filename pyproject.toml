[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "playlist-agent"
version = "0.1.0"
description = "A natural-language playlist curation agent capstone project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]