[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "playlist-agent"
version = "0.1.0"
description = "A goal-driven playlist curation system for constraint-based playlist editing"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]